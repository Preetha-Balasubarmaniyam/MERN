\#Assignment Questions:





\##Answer All This Question



**1. What is the Shortcut Emmet Used to Create boilerplate of HTML ?**

 	Ans: html:5



**2. What is DOCTYPE in Html ?**

 	Ans: It is the declaration that specifies the type and version of the HTML document.



**3. What is Void Element and Example for Void Elements ?**

 	Ans: Void elements do not have close tags. It don't have child elements/content.

 	     They can have attributes to define it.

 	Example: input tag <input>, line break <br>,image tag <img>, meta tag <meta>, link tag <link>

 

**4. What is Difference between Element and Attributes ?**

 	Ans: Element represents the structural or functional part of the webpage. It has opening tag, content(optional), closing tag.

 	     That opening tag contains attributes.

 	     Attributes provides additional information about the element. It modifies the behavior/appearance of an element.

 	     Example: <a href="./products.html"> here a---Element, href---Attribute



**5. What is Html Entries and Why is it need in HTML ?**

 	Ans: HTML entries/entities are special codes to display reserved characters, symbols or invisible spaces in HTML code.

 		It is used in HTML to avoid errors or misinterpretation in HTML as some characters are reserved for HTML like '<' less than symbol is used in opening tag so, to denote less than symbol we use HTML entries, \&lt;



**6. What are meta tag and why is it used?**

 	Ans: Meta tag defines the metadata that specifies the keyword, description and author of the document. It is used for SEO (Search Engine Optimization).It specifies how to display our content and webpage in search engines.



**7. what is the best way to add images in Website ?**

\*\*Ans:\*\* CDN(Content Delivery Network) is the best way to add images in website as it reduces the loading time of the page. We use imagekit.io CDN for adding images and videos.


